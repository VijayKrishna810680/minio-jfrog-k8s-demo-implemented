apiVersion: v1
kind: Service
metadata:
  name: ai-dashboard
  namespace: demo-minio-jfrog
spec:
  selector:
    app: ai-dashboard
  ports:
    - name: http
      port: 8080           # Service port
      targetPort: http     # Container port
  type: ClusterIP
